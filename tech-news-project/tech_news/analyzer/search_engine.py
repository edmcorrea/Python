# from tech_news.database import search_news
# import datetime


# 7
def search_by_title(title):
    """Seu código deve vir aqui"""
    # EXECUTAR O DOCKER DE TESTES
    # EXECUTAR O DOCKER DE TESTES
    # EXECUTAR O DOCKER DE TESTES
    # EXECUTAR O DOCKER DE TESTES
    # EXECUTAR O DOCKER DE TESTES
    # EXECUTAR O DOCKER DE TESTES

    # query = {
    #     "title": {
    #         "$regex": title,
    #         "$options": "i"
    #     }
    # }
    # data = search_news(query)

    # formated_search = [(news["title"], news["url"]) for news in data]

    # return formated_search


# 8
def search_by_date(date):
    """Seu código deve vir aqui"""
    # try:
    #     date_structury = datetime.date.fromisoformat(date)
    # except ValueError:
    #     raise ValueError("Data inválida")

    # formatted_date = date_structury.strftime("%d/%m/%Y")
    # data = search_news({"timestamp": formatted_date})
    # formated_search = [(news["title"], news["url"]) for news in data]
    # # print("______formated_search", formated_search)
    # return formated_search


# 9
def search_by_category(category):
    """Seu código deve vir aqui"""
    # query = {"category": {"$regex": category, "$options": "i"}}

    # data = search_news(query)

    # formated_search = [(news["title"], news["url"]) for news in data]
    # return formated_search
